
If inCurrentTab=1
	Do $cinst.$container().$adjustToolsMenu('Delegate')
	
Else If inCurrentTab=2
	Do $cinst.$container().$adjustToolsMenu('Speaker')
	
Else If inCurrentTab=3
	Do $cinst.$container().$adjustToolsMenu('Arrangement')
	
Else If inCurrentTab=4
	Do $cinst.$container().$adjustToolsMenu('Member')
	
End If